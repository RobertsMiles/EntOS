/**
 * @brief vin - Vi INjured, not a programmer's text editor.
 * 
 * Vin is a text editor that is NOT upwards compatible to Vi.
 * It can read and write to text files, hopefully.
 */
void vin();

/**
 * @brief Prints the welcome message.
 * 
 */
static void welcome();

static void help();

/**
 * @brief User input loop.
 * 
 */
static void input();